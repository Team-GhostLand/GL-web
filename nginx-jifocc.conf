server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    # index is optional — sites may expose only /api, /assets, etc.
    index index.html index.htm;
    autoindex on;
    location / {
        try_files $uri $uri/ =404;
    }
}
